import React from 'react';
import './Crop-Home.css'; // Import the CSS file

const Home = () => {
    return (
        <div className="container text-center">
            <h1 className="header-title">🍁 Plant Disease Detection 🍁</h1>
            <p className="lead">Upload a leaf image and let AI detect potential diseases.</p>
        </div>
    );
};

export default Home;